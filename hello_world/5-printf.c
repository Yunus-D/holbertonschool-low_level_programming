#include <stdio.h>

/**
 * main - Prints a specific message using printf.
 *
 * Description: This function prints exactly the phrase:
 * "with proper grammar, but the outcome is a piece of art,"
 * followed by a new line, using the printf function.
 * It then returns 0 to indicate successful execution.
 *
 * Return: 0.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
